<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- CSS only -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>

    <title>Workout app database</title>
  </head>
  <body style="background-color: #292b2e; color: white">
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item">
          <a href="http://localhost:1337/">Home</a>
        </li>
        <li class="breadcrumb-item active" aria-current="page">Users</li>
      </ol>
    </nav>

    <div class="px-4 py-5 my-5 text-center">
      <h1 class="display-1 fw-bold">Users</h1>
    </div>

    <div class="d-flex justify-content-center">
      <table class="table table-dark rounded" style="width: 75%">
        <thead>
          <tr>
            <th scope="col">#</th>
            <th scope="col">Username</th>
            <th scope="col">Email</th>
            <th scope="col">Date of birth</th>
          </tr>
        </thead>
        <tbody>
          <% users.forEach(function(user, i){ %>
          <tr class="user-row" onclick="location.href='/user/<%=user.username%>'" style="cursor: pointer;">
            <th scope="row"><%=i + 1%></th>
            <td><%=user.username%></td>
            <td><%=user.email%></td>
            <td><%=user.date_of_birth%></td>
          </tr>
          <% }); %>
        </tbody>
      </table>
    </div>

    <!-- <ul>
      <% users.forEach(function(user, i){ %>
      <li onclick="location.href='/user/<%=user.username%>'">
        <%=user.username%> (<%=i + 1%>)
      </li>
      <% }); %>
    </ul> -->
  </body>
</html>
